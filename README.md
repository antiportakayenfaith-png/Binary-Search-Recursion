# Binary-Search-Recursion
This program performs a recursive binary search. It accepts the number of elements, the array elements, and the target value from the user. The array is sorted in ascending order before the binary search is performed.
